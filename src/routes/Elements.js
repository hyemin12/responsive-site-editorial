import Layout from "../components/Layout";
import Title from "../components/elements/Title";

const Elements = () => {
  return (
    <Layout>
      <Title text={"Elements"} size={"2.7em"} padding={"1.5em 0 1em 0"} />
    </Layout>
  );
};

export default Elements;
